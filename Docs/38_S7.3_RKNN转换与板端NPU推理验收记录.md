<!--
文件作用：记录S7.3 RKNN转换、同字节PC基线、板端离线程序和实物验收状态。
覆盖范围：仅RK3568 Linux离线两级手部推理；不含摄像头实时链、手势状态机、虚拟RC、飞控或电机。
当前状态：主机侧准备完成；RK3568未连接，实物误差、性能和30分钟稳定性待验收。
-->

# S7.3 RKNN转换与板端NPU推理验收记录

## 1. 结论

2026-08-13已完成主机侧转换环境、FP16/INT8模型、ARM64板端程序、固定同字节测试语料、PC参考
结果、误差比较和30分钟资源监测工具。RK3568直连地址`169.254.89.120`已能到达SSH服务，但S7.1
结束时按约定移除了一次性公钥，且当前没有COM8可恢复授权，故本记录状态为🟠。交叉编译通过不能
证明板端RKNN执行，PC参考结果也不能代替NPU误差、延迟、内存、温度和稳定性实测。

## 2. 固定环境与模型

- SDK：`/home/user123/rk3568_sdk`。
- Toolkit2：SDK自带`rknn_toolkit2-1.3.0_11912b58-cp38-cp38-linux_x86_64.whl`，
  Python 3.8.20，目标`rk3568`；不自动下载最新版。
- 板端已知Runtime/驱动：1.3.0/0.8.2；本阶段不得替换板端`librknnrt.so`。
- 输入：RGB uint8，转换层执行`(value - 0) / 255`。
- 检测TFLite SHA-256：`60d1bf8d70a80aba35b36290bb2a0e52e784ca2e524937d49ea80e8161a8a384`。
- 关键点TFLite SHA-256：`6acda74af3fbf40e68265c20c7394b2bad81a16a481dcd79ad7a081887c3d6b9`。

最终FP16位于`/home/user123/s7_3_models_final_fp16`：

| 模型 | 字节 | SHA-256 |
|---|---:|---|
| `hand_detector_fp16.rknn` | 2601524 | `aafe382686646bc858f5f90c399a20bb01d9b66dec1e34d19002fa6bf4400f64` |
| `hand_landmarks_detector_fp16.rknn` | 5819761 | `f34abcd8ab87e972dfb18357d3bb894eea1764fa26da3942ae8bcc2be91c9fba` |

最终INT8位于`/home/user123/s7_3_models_final_int8`。校准集从固定语料均匀确定性抽取100帧，
其中28帧经正确检测模型形成真实关键点ROI：

| 模型 | 字节 | SHA-256 |
|---|---:|---|
| `hand_detector_int8.rknn` | 1484443 | `4ab89b23f3a52b18395e812e990767fdd021801929607be9ab3f69ba414f9391` |
| `hand_landmarks_detector_int8.rknn` | 3092210 | `eeb46a0769eb32fe14836bb25f70b18d95771f3fe5b4a70d955ce91e13466dcc` |

转换日志中的输入/输出改为INT8是量化模型的预期告警；`Pad: mode constant only support 2 dims pads!`
为编译器信息提示，模型已成功生成，但仍必须由板端误差结果判断是否可交付。

## 3. 固定语料与PC基线

为排除PC与板端视频解码器差异，原S7.2图片和视频全部展开为JPEG质量100的逐帧语料。原测试集
有1条损坏媒体记录，继续保留为`invalid_image`边界，不伪造成可推理帧。

- 语料目录：`C:\Users\user\AppData\Local\Temp\s7_3_fixed_testset`。
- 可解码帧：2859；清单SHA-256：`f819f54b745eecabf148c111115174aabd9fef763dd47d0901b399c1ff37f9ed`。
- PC结果：723条有效、2136条拒绝；JSONL SHA-256：
  `c08aa45e3c5574ed216f332b0a93848f190679a9fc51aa3d68082180fe4e7777`。

## 4. 板端程序与冻结关口

`Tools/s7_3/s7_3_board_infer`已交叉构建为ARM64 ELF，SHA-256为
`ee8befa51d8341e2eeb313620d389e10d6ef114ea31eaa5e94ddaaf8456d0cfd`。程序复用S7.2的
192×192检测、加权NMS、旋转ROI和224×224关键点合同，逐帧输出JSONL与分段耗时。私有运行库仅
包含OpenCV 4.5.5的`core/imgproc/imgcodecs`，不引入摄像头、GUI或实时视频能力。

已生成一次性部署包`/home/user123/s7_3_deploy_20260813.tar.gz`，共1404366742字节、2885个条目，
`gzip -t`通过，SHA-256为`bfe5d236914b3c5ed7543d78fa4516360851792729a804e5ecf7742493ce31b4`。
该包只作为板端验收输入，不进入飞控仓库。

上板前冻结下列范围，不根据结果反向调宽：有效/拒绝一致率不低于99%；检测分数和存在/手性分数
P95绝对误差不大于0.05；归一化框和检测关键点P95绝对误差不大于0.02；归一化图像关键点P95
绝对误差不大于0.03。FP16和INT8分别测试；INT8不满足则降级交付FP16，不修改S7.2语义门槛。

30分钟验收由`Tools/s7_3/board_stability.sh`保持同一进程和RKNN上下文连续循环，每10秒记录RSS、
文件描述符、温度和可用的NPU负载。验收要求无持续RSS/句柄增长、无设备重置、无推理错误、无结果
冻结；同时从JSONL统计预处理、检测NPU、后处理、关键点NPU的平均、P95和P99。

## 5. 待补实物证据

重新连接ATK-DLRK3568后，只执行以下S7.3项目：

1. 核对板端Runtime 1.3.0、驱动0.8.2、温度节点和NPU负载节点。
2. 部署二进制、三项私有OpenCV库、固定语料及FP16/INT8模型，不覆盖系统Runtime。
3. 分别运行完整2859帧并用`compare_results.py`生成误差/延迟报告，归档失败样本。
4. 对候选交付模型运行1800秒稳定性脚本，归档`results.jsonl`、`runtime.log`和`resources.csv`。
5. 只有全部冻结关口通过后，才把S7.3改为✅并允许进入S7.4。
