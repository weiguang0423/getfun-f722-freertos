<!--
文件作用：冻结 S7.6 虚拟 RC 软件契约、协议边界、当前结果和实物验收方法。
覆盖范围：Linux 手势映射、限幅限速、二进制帧与发送；不含飞控 RC 仲裁和电机控制。
关联模块：Linux/RK3568/s7_5、Linux/RK3568/s7_6、Docs/01、Docs/33、Docs/40。
验收结论：开发者于 2026-08-22 明确确认事件式新版 S7.6 验收成功；原始软件结果和实物步骤保留供追溯。
-->

# S7.6 手势到虚拟 RC 映射与发送链路开发验收

> 当前状态：✅ 2026-08-22 事件式映射、1 s 单次命令和健康中立帧已确认验收通过

## 1. 已确认的入口与安全边界

开发者已明确确认 S7.5 验收通过，本阶段不重复检查 S7.5 实物数据。S7.6 只消费 S7.5 的
`GestureSnapshot`，生成 Linux 候选帧。STM32 USART6 已增加只读帧监视，用于通过 UART4 确认收字节、
帧边界、CRC 和字段；源仲裁仍留在 S7.7，因此本阶段不能把候选值直接
写入 setpoint、PID、Mixer 或 DShot，也不能生成 ARM 和总授权通道。

硬件文档 01 已确认飞控 UART2 被 CR8 ELRS/CRSF 使用；本次已将飞控 Linux 通信/诊断物理口切换为
USART6（PC6=TX、PC7=RX）。RK3568 侧实际 UART 设备、电平和接线仍需实物核对，程序通过
`S7_6_RC_OUTPUT` 显式选择设备，禁止默认复用或并接 UART2。串口参数冻结为 115200 8N1。

## 2. 映射与失效契约

映射改为事件式短命令：张掌为 Pitch -0.30（低头前向），单指为 Pitch +0.30（抬头后向），
V 字为 Roll -0.30（左向），握拳为显式释放。每次进入 `ACTIVE` 只触发一次 1 s 目标
脉冲，目标保持期结束后自动回中；持续保持手势不重复触发。Yaw、Throttle 和 AUX 当前保持释放值。
该语义是有界姿态脉冲，没有位置/速度反馈时不承诺精确移动距离。

通道整数尺度、范围和变化率：

| 通道 | 中立/释放 | 合法范围 | 最大变化率 | 首版实际目标 |
|---|---:|---:|---:|---:|
| Roll | 0 | -300～+300 | 600/s | 0 或 -300 |
| Pitch | 0 | -300～+300 | 600/s | -300、0 或 +300 |
| Yaw | 0 | -300～+300 | 600/s | 0 |
| Throttle | 0 | 0～250 | 400/s | 0 |
| AUX | 0 | 0～1000 | 1000/s | 0 |

有效动作按变化率接近目标并自动回中。摄像头和推理管线健康时，`NO_HAND/UNKNOWN/RELEASED/CANDIDATE`
持续发送 `valid=1`；已经触发的命令锁存到 1 s 目标期结束，随后全通道回中，因此不会被转换帧截短，
也不会触发飞控退出和重新授权。健康中立帧固定使用 `gesture_id=UNKNOWN`、置信度 0 和全通道 0。
握拳、deadline miss、采集/推理失败或时间异常则立即发送无效释放。

- S7.5 状态为 `ACTIVE`，观测 ID 等于活动 ID，置信度不低于 0.75；
- 正常空档中，未结束的命令沿用上一个手势 ID 与置信度；命令结束后改为有效 UNKNOWN 中立；`FIST` 仍为无效释放；
- 源和发送时间均为单调时钟，时间未回退，源年龄不超过 250 ms。

健康启动第一帧为有效 UNKNOWN 中立；串口写失败抛错终止进程，不自动重连。进程重启会改变接收会话，
活动中的飞控立即退出并要求 AUX3 重新低高授权，因此旧动作不会自动恢复。

## 3. 帧与接收拒绝规则

固定帧共 44 字节，包含魔数/版本/帧长、有效位、手势 ID、置信度、源序号、发送心跳、源/发送
时间戳、五个 `int16` 通道和 CRC16-CCITT-FALSE。实时链沿用 S7.4/S7.5 已冻结的 20 fps，因此
正常发送周期为 50 ms。源序号允许因最新帧策略跳号但必须前进，心跳每次发送加一。

`decode()` 已实现下一阶段接收边界的纯逻辑参考。以下任一情况必须拒绝：

- 魔数、版本、帧长、保留位或 CRC 错误；
- 任一通道超出冻结范围；
- 心跳未前进，或有效帧源序号未前进；
- 接收时距发送超过 150 ms；
- 有效帧的源结果年龄超过 250 ms；
- 无效帧仍携带非零通道；有效 UNKNOWN 帧不是置信度 0/全通道 0；或有效帧携带 FIST。

## 4. 当前软件结果

- x86 WSL 使用 C++17、`-Wall -Wextra -pedantic` 编译并运行纯逻辑自检，输出
  `s7.6 virtual RC self-test ok`。
- 同一环境通过临时文件运行真实 `SerialLink::send` 写出与两帧解码回环，输出
  `s7.6 serial loopback self-test ok`；这证明软件写出路径，不代替 UART 电气验收。
- 新自检覆盖三个事件映射、保持不重复触发、1 s 目标保持期后回中、转换期保持有效、
  Throttle/AUX 保持释放、编解码往返、CRC/时间/序号拒绝和握拳立即释放。
- 旧持续量版 ARM64 ELF（112992 字节，SHA-256
  `61cfff8d59ed8ce28f42836fdfc875cef54ae01f3ca01bcda38135adac9933fb`）已因映射契约变更而失效，新版待重新交叉构建。
- x86 WSL 执行 ARM64 产物会得到 `Exec format error`，这是主机架构边界，不是板端自检结果。

## 5. 已关闭的实物关口（保留原过程供追溯）

1. 在 RK3568 执行 `s7_6_live --self-test`，返回码为 0。
2. 核对 RK3568 UART 引脚、电平和飞控空闲 UART；不得与 UART2/CRSF 两发送端并接。
3. 用串口回环或逻辑分析仪验证 115200 8N1、44 字节、20 Hz、CRC、源序号和心跳。
4. 四种手势、无手、未知、低置信度、遮挡和快速切换逐项核对 `virtual_rc_*` JSON 与线上帧。
5. 验证轴限幅和 600/s 变化率；Throttle/AUX 始终为 0，握拳和任一故障当前帧立即全释放。
6. 拔线、杀死进程和重启：接收观察端在 150 ms 内判为链路失效，恢复后不得自动恢复动作。

开发者于 2026-08-22 明确确认以上事件式新版 S7.6 验收成功，并要求将 S7.9 之前阶段统一冻结。
该确认关闭映射与发送链路阶段；S7.7/S7.8 的验收结论分别记录在文档 42/43。
