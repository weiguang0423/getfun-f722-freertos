文件作用：记录S4.1～S4.3软件交付证据，并提供一次完成的逻辑分析仪与四路无桨验收表。  
安全边界：所有步骤必须拆除全部螺旋桨；未通过本文件前不创建v1.0.0标签。  

# v1.0.0 S4.1～S4.3软件交付与联合实物验收

> 当前结论：2026-08-10开发者确认S4.1～S4.3联合实物验收通过。最终实现采用TIM8_CH1节拍、DMA2 Stream2/Channel7直写GPIOA BSRR的四路DShot600 GPIO bitbang路径。
> 冻结标签：`v1.0.0-motor-output-baseline`

## 1. 软件交付

| 文件 | 作用 |
|---|---|
| `APP/Src/rtos/flight_task.c` | 1 kHz一致快照、新鲜度门禁、测试超时和停止提交 |
| `APP/Src/bsp/dshot_motor.c` | 四路DShot600编码、Betaflight F7 GPIO bitbang和故障低电平 |
| `APP/Inc/app_state.h` / `APP/Src/app_state.c` | Flight/DShot一致诊断快照 |
| `APP/Src/protocol/msp_server.c` | MSP2 `0x4005`状态与`0x4006`测试写入 |
| `Tools/verify_flight_motor.mjs` | S4.1～S4.3独立离线回归 |
| `Tools/motor_test.ps1` | 兼容Windows PowerShell 5.1的单路低值测试、运行状态/错误增量检查与finally停止保护 |

## 2. 软件证据

- [x] `verify_attitude_estimator.mjs` PASS。
- [x] `verify_imu_timing.mjs` PASS。
- [x] `verify_imu_filter.mjs` PASS。
- [x] `verify_crsf.mjs` PASS。
- [x] `verify_rc_failsafe.mjs` PASS。
- [x] `verify_power_monitor.mjs` PASS。
- [x] `verify_flight_motor.mjs` PASS。
- [x] Debug clean build：RAM 46,408 B，FLASH 116,732 B。
- [x] Release clean build：RAM 46,408 B，FLASH 66,300 B。
- [x] PARAM_A/PARAM_B链接占用均为0 B。
- [x] `git diff --check`通过。
- [x] 2026-08-09首测后调整：TIM1三路DMA burst曾启用full FIFO；测试脚本改用`Stopwatch`，不再因`TickCount64`为空而常驻。
- [x] 2026-08-09第二次日志定位：约12 ms已出现`dshot=0 dma_err=1`，约178.6 s后`test=1`，故输入链已通、故障位于启动首帧DShot DMA。
- [x] 2026-08-09第三次日志：`dma_err=1 dma_flags=[0x00000800,0x00000800]`；`0x800`是主动关闭DMA生成的TC，证明原“最后中断标志”会覆盖真正故障。
- [x] 2026-08-09第四次日志：启动约12 ms即`dma_flags=[0x00000040,0x00000000]`；该版虽为32位/full FIFO，但TIM1只配置三项burst，与原始Betaflight固定四项burst不同，随后曾临时撤销。
- [x] 临时16位direct mode消除了DMA报错，但主机最终状态虽为`test=1 dshot=1 out=[80,0,0,0]`，电机仍无反应；DMA TC不能证明CCR获得正确逐位序列。
- [x] 2026-08-09第五次日志：首条`flight`为`dshot=1 busy=1 submit_err=0 dma_err=0`；下一周期日志为`dshot=0 submit_err=1129 dma_err=0`，证明首帧启动成功后由非DMA错误路径锁止。
- [x] DMA共享IRQ改为只写回本次读取的`tim1_flags`/`tim2_flags`，不再用`*_DMA_ALL_FLAGS`清除快照读取后才到达的TCIF。
- [x] 2026-08-09第六次日志：`flight ready=1 safety=0 dshot=0 dma_err=0`，同时`params save=1 accel cal=1`；定位为校准保存前的旧GPIO停机调用破坏DShot AF模式。
- [x] 删除ImuTask中的`platform_motor_outputs_force_safe()`调用；校准状态自身保持arming inhibit并让FlightTask提交零值，ImuTask不再接管电机引脚。
- [x] 测试脚本修正PowerShell空数组折叠导致的MSP2空回包CRC误判，并核对运行期间的测试状态、实际输出及错误增量。
- [x] TIM1/TIM2均改为32位、full FIFO、CCR1～CCR4四项DMAR burst；未使用CCR写0，FEIF/DMEIF/TEIF均恢复为致命DMA错误。
- [x] 2026-08-09实物测试：M1值200持续3秒时命令、输出与DMA计数均正常，但电机只鸣叫；确认零油门帧已持续发送，排除ESC预解锁序列缺失。
- [x] 原始GETFUNF722V3未指定协议并继承Betaflight默认DShot600。
- [x] 原始目标未覆盖`DEFAULT_DSHOT_BITBANG`；STM32F7的`AUTO`实际启用bitbang，而非此前自写的TIM1/TIM2 DMAR路径。
- [x] 按Betaflight默认资源移植：TIM8_CH1以1.8 MHz产生三相节拍，DMA2 Stream2/Channel7把51个word写入GPIOA BSRR，同时驱动PA15/PA10/PA9/PA8。
- [x] 新候选离线验证与Debug构建通过；实物旋转结果仍为空，不把编译通过写成电机已修复。

```text
Debug BIN SHA-256:
23004257E6F121394F831D77BC0EC52FB66585AB0BA2C712CF5F42F49476D240

Release BIN SHA-256:
76A4BF2BE4B9E234F1CD27B2CD460F4E794A406E44279E610D1E4FADEC9AA6EA
```

## 3. 烧录与接线前检查

- [x] 四个电机的螺旋桨全部拆除，工作区没有散落螺旋桨。
- [x] 首先只接逻辑分析仪，不给ESC动力；M1～M4地线与分析仪共地。
- [x] 使用本轮Debug BIN并核对上面的SHA-256；UART启动行应为`BUILD: Aug  9 2026 23:23:58`。
- [x] UART4保持115200 8N1且只用于接收日志；USB CDC端口只由测试脚本占用，关闭Betaflight App。
- [x] RC、IMU校准/参数和电池均正常，UART的总 `inhibit=0x00000000`。
- [x] `flight ready=1 dshot=1`，`submit_err=0 dma_err=0 dma_flags=0x00000000`，FlightTask栈余量大于128 words。
- [x] `dshot`、`dma_err`和`submit_err`均满足继续测试条件。

## 4. 逻辑分析仪验收

先保持四路测试值为0，检查PA15、PA10、PA9、PA8：

- [x] 四路位速率均为600 kbit/s，单bit约1.667 us。
- [x] 0高电平约0.556 us，1高电平约1.111 us。
- [x] 每帧16位MSB优先，后面三个0.556 us低保持状态；1 kHz周期重复。
- [x] 停止帧为 `0x0000`；值48的帧为 `0x0606`，checksum正确。
- [x] 四路由同一个GPIOA BSRR DMA同步更新，起始边沿无可见偏差，帧内无毛刺或被其他DMA破坏。
- [x] 同时运行IMU、CRSF、ADC3和USB时，所有DMA错误计数不增长。

记录：

```text
M1 PA15: 验收通过（具体测量值未随本次确认提供）
M2 PA10: 验收通过（具体测量值未随本次确认提供）
M3 PA9 : 验收通过（具体测量值未随本次确认提供）
M4 PA8 : 验收通过（具体测量值未随本次确认提供）
四路最大起始偏差：验收通过（具体测量值未随本次确认提供）
```

## 5. 四路无桨测试

确认拆桨后，关闭占用USB端口的App。在仓库根目录的Windows PowerShell中执行，不能粘贴到
UART4串口终端；每次只测试一台电机，从值80、1秒开始：

```powershell
.\Tools\motor_test.ps1 -Port COM7 -Motor 1 -Value 80 -DurationMs 1000 -PropsRemoved
.\Tools\motor_test.ps1 -Port COM7 -Motor 2 -Value 80 -DurationMs 1000 -PropsRemoved
.\Tools\motor_test.ps1 -Port COM7 -Motor 3 -Value 80 -DurationMs 1000 -PropsRemoved
.\Tools\motor_test.ps1 -Port COM7 -Motor 4 -Value 80 -DurationMs 1000 -PropsRemoved
```

也可用 `-Values` 四路同时测试（1～4号按序，值0为停止、48～1000有效）：

```powershell
.\Tools\motor_test.ps1 -Port COM7 -Values 80,80,80,80 -DurationMs 1000 -PropsRemoved
```

把 `COM7`替换为飞控的USB CDC端口，不是UART4外接USB-TTL的端口。脚本在运行阶段每100 ms刷新一次命令，
并逐帧检查飞控MSP回执；若`flight ready`、`dshot`或`safety`不允许输出，会立即报错而不是静默运行。
`finally`连续写入三次停止帧但不等待其回执；值被限制在48～1000，且未显式传入 `-PropsRemoved` 时拒绝运行。
正常运行时应立即显示测试提示，到达`DurationMs`后显示停止保护写入次数并返回PowerShell提示符；
若三次停止写入全部失败，脚本会明确报错，固件仍应在停止刷新后250 ms内自动归零。

| 逻辑编号 | 焊盘/引脚 | 实际机架位置 | 旋转方向 | 结果 |
|---|---|---|---|---|
| M1 | PA15 | 已核对，具体位置未录入 | 已核对，具体方向未录入 | 通过 |
| M2 | PA10 | 已核对，具体位置未录入 | 已核对，具体方向未录入 | 通过 |
| M3 | PA9 | 已核对，具体位置未录入 | 已核对，具体方向未录入 | 通过 |
| M4 | PA8 | 已核对，具体位置未录入 | 已核对，具体方向未录入 | 通过 |

验收项：

- [x] 每次只有指定电机转动，其他三台保持停止。
- [x] 四个逻辑编号与焊盘和实际机架位置一一对应。
- [x] 已核对四台旋转方向；本阶段未通过软件猜测或自动反向。
- [x] 脚本正常结束后电机立即停止。
- [x] 测试中强制终止脚本或拔USB，停止刷新后不超过250 ms全部停止。
- [x] RC断链达到300 ms后，下一FlightTask周期提交停止值；恢复RC不会自动恢复旧命令。
- [x] IMU样本停更超过5 ms或任一inhibit出现时，下一周期提交停止值。
- [x] 非法值1～47或大于2047被拒绝，不发送DShot保留命令。

## 6. 稳定性与回归

- [x] 四路停止帧、IMU 1 kHz、CRSF、ADC 50 Hz和USB同时运行30分钟。
- [x] `flight loops`持续约每秒增加1000，`missed`无持续增长。
- [x] `submit_err=0`、`dma_err=0`，IMU/RC/ADC既有错误计数无异常增长。
- [x] FlightTask栈余量、ImuTask、RcTask、BatteryTask栈余量均大于128 words。
- [x] M5～M8始终保持GPIO低电平。
- [x] 重启、受控故障和Flash保存路径不会把M1～M4从GPIO安全低电平重新拉起。

## 7. 最终记录

```text
日期：2026-08-10
开发者：用户确认
Debug BIN SHA-256：23004257E6F121394F831D77BC0EC52FB66585AB0BA2C712CF5F42F49476D240
逻辑分析仪：通过，具体设备与测量值未随本次确认提供
ESC/电机/电池：通过，具体型号未随本次确认提供
M1～M4位置与方向：已核对，具体映射未随本次确认提供
250 ms超时结果：通过
IMU/RC失效结果：通过
30分钟结果：通过
异常与处理：最终采用TIM8_CH1 + DMA2 Stream2/Channel7 GPIO bitbang路径
结论：通过
```

2026-08-10开发者确认本文件结论为“通过”，允许更新S1.6和S4.1～S4.3为✅，
提交并创建`v1.0.0-motor-output-baseline`。具体逻辑分析仪数值、电机位置和旋转方向仍应补录，
但不以软件构建结果替代本次开发者确认的实物验收结论。
