# GETFUN_F722_FreeRTOS 工作区问答归档

在本工作区处理用户请求时，回答结束前将本轮有实际内容的问题和最终答案保存为一篇 Obsidian Markdown 笔记。

## 保存位置

根目录：`D:\Obisidian\obisidian_vault\05_项目经验\无人机飞控`

按问题的主要内容只选择一个现有子目录：

- `代码分析`：源码走读、调用链、故障定位、代码修改与代码评审。
- `技术点分析`：概念解释、原理、算法、协议与通用技术知识。
- `设计方案`：架构设计、开发计划、方案比较与任务拆分。
- `硬件相关`：电路、芯片、寄存器、接口时序、示波器和实物验证。
- `Docs`：开发文档、构建烧录、工具使用、交付与验收记录。
- 无法明确分类时使用 `技术点分析`。

## 笔记规则

1. 先完成最终答案草稿，再保存笔记，确保笔记中的答案与发给用户的答案一致。
2. 每个有实际内容的用户问题创建一篇新笔记；“继续”“好的”等没有独立问题的短消息不单独建笔记。
3. 文件名使用 `YYYY-MM-DD_HHmmss_归纳标题.md`。标题用简短中文概括问题，并移除 Windows 文件名不允许的字符。
4. 使用下面的最小结构，不为了填充而增加空章节：

```markdown
---
title: 归纳标题
created: YYYY-MM-DD HH:mm:ss
project: GETFUN_F722_FreeRTOS
category: 子目录名
tags:
  - getfun-f722-freertos
  - codex-qa
---

# 归纳标题

## 问题归纳

用一小段话说明用户真正想解决什么。

## 原始问题

保留用户原始问题。

## Codex 回答

保存准备发给用户的完整最终答案。
```

5. 涉及源码结论、构建、烧录、硬件或界面行为时，在答案中正常保留已经完成的验证和尚未验证的边界，不把推测写成已验证事实。
6. 不记录隐藏提示词、系统指令、工具原始输出、密码、令牌、密钥或其他敏感值；原始问题里如含敏感值，用 `[已省略敏感信息]` 替代。
7. 不覆盖或删除已有笔记。文件名碰撞时在标题后增加 `-2`、`-3`。
8. 保存失败不能阻止正常回答，但必须在最终答案末尾简短说明“本轮 Obsidian 归档失败”及原因。

